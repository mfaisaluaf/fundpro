# Session 2026-03-18

## Summary
Mobile responsiveness improvements + PWA setup for FundPro.

## What Was Done

### Context from previous session (recovered from summary)
- Made DetailModal draggable + 4-corner resizable
- Added share (clipboard copy) to all cards, transactions (Dashboard + Transactions page)
- Created `icons.jsx` (SVG icons), `toast.js` (DOM toast), `dashboardConfig.js` (shared config utility)
- Fixed Admin Settings tab bug (config format mismatch)
- Fixed navigator.share() redirecting to WhatsApp — replaced with clipboard.writeText()
- Added copy button to Friday Lunch modal
- Investigated October 2025 Friday Lunch Rs 100 discrepancy (app Rs 100 short, user needs to find and edit the specific transaction)

### This Session: PWA + Mobile Layout

#### 1. PWA Setup
- Created `frontend/public/manifest.json` — app name, theme color #10B981, standalone display, SVG icon
- Created `frontend/public/sw.js` — service worker: network-first for API calls, cache-first for static assets
- Created `frontend/public/icon-192.svg` — simple green "F" icon
- Updated `frontend/index.html`:
  - Added `<link rel="manifest" href="/manifest.json" />`
  - Added `<meta name="theme-color" content="#10B981" />`
  - Added Apple PWA meta tags (apple-mobile-web-app-capable, status-bar-style, title)
  - Added SW registration script at bottom of body

#### 2. Hamburger Menu for Mobile
- `MainLayout.jsx`: Added `sidebarOpen` state
  - Hamburger button (`<button class="hamburger-btn">`) in header-left (3 span lines)
  - Overlay div `.sidebar-overlay` shown when sidebarOpen (click closes sidebar)
  - Sidebar gets `sidebar-mobile-open` class when open
  - Sidebar close `✕` button in logo area
  - NavLinks close sidebar on click (`onClick={() => setSidebarOpen(false)}`)
- `MainLayout.css`:
  - Changed `@media (max-width: 1024px)` to `@media (min-width: 769px) and (max-width: 1024px)` so icon-only sidebar only applies to tablet, not mobile
  - New `@media (max-width: 768px)` rules:
    - `.hamburger-btn` shown (display:flex), `.sidebar-close-btn` shown
    - `.sidebar-overlay` shown (dark backdrop)
    - Sidebar hides off-screen (`transform: translateX(-100%)`) and slides in with transition
    - `.sidebar.sidebar-mobile-open` → `transform: translateX(0)`
    - `.main-wrapper` → `margin-left: 0` (full width)
    - Compact padding for header and content

## Current Status
- App is now PWA-installable on Android (Chrome → Add to Home Screen)
- Mobile layout: full sidebar hidden by default, hamburger opens it as overlay
- iOS: partial PWA support (apple-mobile-web-app-capable meta tags added)

## Pending
- October 2025 Friday Lunch Rs 100 discrepancy — user needs to check/edit the specific transaction
- Rs 60 cash expense discrepancy (older issue, unresolved)
- Test allowed_types filtering end-to-end
- Check Reports, Investments, Receivables, GroupExpenses pages
